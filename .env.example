# Frontend Environment Variables (Safe to be public)

# API URL mapping (to Docker container or localhost)
VITE_API_URL="http://localhost:8000"

# Supabase Anon Key (for public client interactions if needed later)
VITE_SUPABASE_URL="https://your-project-id.supabase.co"
VITE_SUPABASE_ANON_KEY="eyJhb...replace_with_public_anon_key..."